[package]
name = "ambient-onboard"
version = "0.1.0"
edition = "2021"

[dependencies]
ambient-core = { path = "../ambient-core" }
ambient-store = { path = "../ambient-store" }
ambient-query = { path = "../ambient-query" }
chrono = "0.4"
