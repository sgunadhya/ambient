[package]
name = "ambient-stream"
version = "0.1.0"
edition = "2021"

[dependencies]
ambient-core = { path = "../ambient-core" }
chrono = "0.4"
serde_json = "1"
sqlite = "0.32"
tokio = { version = "1", features = ["sync"] }

[dev-dependencies]
tempfile = "3"
