[package]
name = "ambient-transport-google-health"
version = "0.1.0"
edition = "2021"

[dependencies]
ambient-core = { path = "../ambient-core" }
chrono = "0.4"
tokio = { version = "1", features = ["rt"] }
